name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy-frontend:
    name: Deploy Frontend to Server
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Deploy dist/ to server via SSH
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: frontend/dist/
          target: ${{ secrets.SSH_DEPLOY_PATH }}
          strip_components: 2   # removes "frontend/dist" prefix, copies contents directly

  # ── Backend deploy (uncomment when Spring Boot backend is ready) ─────────────
  # deploy-backend:
  #   name: Deploy Backend to Server
  #   runs-on: ubuntu-latest
  #   needs: deploy-frontend
  #
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - uses: actions/setup-java@v4
  #       with:
  #         java-version: '17'
  #         distribution: temurin
  #         cache: maven
  #
  #     - name: Build JAR
  #       run: mvn -f backend/pom.xml package -DskipTests
  #
  #     - name: Copy JAR to server
  #       uses: appleboy/scp-action@v0.1.7
  #       with:
  #         host: ${{ secrets.SSH_HOST }}
  #         username: ${{ secrets.SSH_USER }}
  #         key: ${{ secrets.SSH_PRIVATE_KEY }}
  #         source: backend/target/*.jar
  #         target: /opt/werewolf/
  #         strip_components: 2
  #
  #     - name: Restart backend service
  #       uses: appleboy/ssh-action@v1.0.3
  #       with:
  #         host: ${{ secrets.SSH_HOST }}
  #         username: ${{ secrets.SSH_USER }}
  #         key: ${{ secrets.SSH_PRIVATE_KEY }}
  #         script: sudo systemctl restart werewolf-backend
